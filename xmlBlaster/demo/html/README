Howto run the html examples with persistent http connection:
============================================================

1. Set the DocumentRoot of your web server to this directory
   for Apache e.g.:

   DocumentRoot "/home/ruff/xmlBlaster/demo/html"
    

2. Add the servlet to you servlet env, with jserv in zone.properties:

   servlet.SystemInfo.code=demo.html.systemInfo.SystemInfo
    

3. Use the Makefile or do it manually:
	cp -r  ../../src/java/org/xmlBlaster/protocol/http/javascript  javascript
	cp -r  ../../src/java/org/xmlBlaster/protocol/http/persistentWindow  persistentWindow


4. Start xmlBlaster


5. Start a publisher, so that we have some dynamic data:
	
	java demo.html.systemInfo.SystemInfoPublisher


6. Start you browser

   http://localhost/systemInfo/systemInfo.html?ActionType=login&loginName=martin&passwd=secret



jserv > /dev/null 2>&1 &
java org.xmlBlaster.Main -logFile /tmp/xmlBlaster.log  -useKeyboard false  > /dev/null 2>&1 &
java demo.html.systemInfo.SystemInfoPublisher -info false > /dev/null 2>&1 &
